// Template Literals
let word1 = "Dylan"
let word2 = "Kim"

const fullName = `${word1} ${word2}`

// Destructuring Objects
const player = {
    name: "Conor",
    team: "Blue",
    address: {
        city: "LA"
    }
}

let { name, team, address } = 